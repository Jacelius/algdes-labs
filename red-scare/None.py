# return the length of the shortest s, t path
# If no such path exists, return ‘-1’.
# has to avoid red nodes (nodes in R)
def shortest_path(G, s, t, R):
    # idea: remove red nodes from G, then run shortest path algorithm
    pass
